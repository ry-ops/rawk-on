{
  "name": "rawk-on",
  "version": "0.1.0",
  "description": "Hover a song on The Current's playlist and add it to a daily TIDAL playlist.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0",
    "@types/chrome": "latest",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
